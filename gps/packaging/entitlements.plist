<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!--
      Hardened-runtime entitlements for the Developer-ID / notarized agent.

      The agent is NOT sandboxed (App Store sandbox forbids the device I/O this needs —
      usbmux, RSD tunnels, DDI mount). It talks to usbmuxd over its unix socket and opens
      network sockets to the device/overlay; unsandboxed hardened-runtime processes get
      those by default, so no sandbox network entitlement is required here.

      We keep the grant surface minimal. If notarization or the hardened runtime later
      rejects something (e.g. a dependency that maps writable+executable memory), add the
      narrowest matching key — do not broaden pre-emptively.
    -->
    <key>com.apple.security.cs.allow-jit</key>
    <false/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <false/>
</dict>
</plist>
